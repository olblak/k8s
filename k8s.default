#!/bin/bash

: "${PREFIX:=jenkinsci}"
: "${LOCATION:=eastus}"
: "${ENV:=staging}"
# Following keys should not be override
: "${CONFIGURATIONS_PATH:=./resources/configurations/$ENV/}"
: "${DEFINITIONS_PATH:=./resources/definitions}"
: "${SYSTEMS_PATH:=./resources/systems}"
